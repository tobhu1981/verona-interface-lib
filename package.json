{
  "name": "verona-monorepo",
  "version": "1.0.0-beta",
  "private": true,
  "description": "Verona Interface Libraries Monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "doc:build": "pnpm --filter @verona/player doc && pnpm --filter @verona/editor doc",
    "doc:player": "pnpm --filter @verona/player doc",
    "doc:editor": "pnpm --filter @verona/editor doc",
    "doc:combined": "typedoc",
    "doc:serve": "pnpm doc:combined && npx serve docs",
    "publish:all": "pnpm -r publish",
    "test": "pnpm -r test"
  },
  "keywords": [
    "verona",
    "monorepo",
    "player",
    "editor",
    "schemer",
    "widget"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3",
    "tsup": "^8.0.1",
    "typedoc": "^0.26.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}