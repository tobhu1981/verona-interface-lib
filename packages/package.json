{
  "name": "verona-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Verona Interface Libraries Monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "docs": "pnpm -r docs",
    "docs:combined": "typedoc",
    "docs:serve": "pnpm docs:combined && npx serve docs",
    "publish:all": "pnpm -r publish",
    "test": "pnpm -r test"
  },
  "keywords": [
    "verona",
    "monorepo",
    "player",
    "editor",
    "schemer",
    "widget"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3",
    "tsup": "^8.0.1",
    "typedoc": "^0.25.7"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}